---
interface Props {
  size: "small" | "medium" | "large";
}

const { size = "medium" } = Astro.props;

const sizeClasses = {
  small: "h-8 md:h-12",
  medium: "h-16 md:h-24",
  large: "h-24 md:h-40",
};
---

<div class={sizeClasses[size]} aria-hidden="true"></div>
